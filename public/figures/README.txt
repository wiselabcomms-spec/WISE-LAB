Drop the two hero silhouettes here (exact filenames):

  founder.png     — the girl with the LAPTOP               (Startup / Founder path, blue)
  enterprise.png  — the girl with the SHIRT ON A HANGER     (Business / Enterprise path, orange)

Notes
- PNG or JPG both work; a white or transparent background is fine
  (the dark silhouette is what gets sampled into particles).
- Keep both images the SAME framing/scale if possible — since it's the same
  woman in the same stride, that keeps her body registered so the morph reads
  as her swapping the object rather than two different shapes.
- Higher-contrast, cleanly-cut silhouettes sample best. Roughly 800–1600px is
  plenty.

Once these two files exist, the hero automatically switches from the procedural
placeholder figure to your artwork — no code change needed.
